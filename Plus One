class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        s=''
        l=[]
        for i in range (len(digits)):
            s+=str(digits[i])
        n=int(s)+1
        st=str(n)
        for i in st:
            l.append(int(i))
        return l
        
